<%= snippet 'tomorrow_menu_mail_header'%>
<% for menu in @menus %>
  <h3><%= menu.name.mb_chars.capitalize %></h3>
  <% for meal in menu.meals %>
    <%= render :partial => 'meal', :locals => {:meal => meal} %>
  <% end %>
  <h4><%= t(:whole_menu)%></h4>
  <p><%= t(:price)%>: <strong><%= format_currency(menu.discount_price(:date => @date, :user => @user), :template => @currency_template) %></strong></p>
<% end 
   for category in @categories %>
  <h3><%= category.name.mb_chars.capitalize %></h3>
  <% for meal in category.meals %>
    <%= render :partial => 'meal', :locals => {:meal => meal} %>
  <% end 
   end %>
