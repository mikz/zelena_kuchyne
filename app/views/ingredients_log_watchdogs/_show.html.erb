<td><%= link_to format( record.ingredient.name ), :action => 'show', :id => record %></td>
<td><%= format(locales["operator_#{record.operator_readable}"]) %></td>
<td><%= format_amount(record.value, :unit => record.ingredient.unit ) %></td>
<td>
  <%= smart_link locales[:edit], :action => 'edit', :id => record %>
  | <%= smart_link locales[:delete], {:action => 'destroy', :id => record}, :confirm => locales['delete_confirmation'] %>
</td>