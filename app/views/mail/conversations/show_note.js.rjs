if(@conversation.note == '')
  page.remove "#conversation_#{@conversation.id} tr.conversation-note-editor"
else
  page.replace("#conversation_#{@conversation.id} tr.conversation-note-editor", :partial => 'note', :locals => {:note => @conversation.note, :id => @conversation.id})
end